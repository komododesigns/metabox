
{% set enq = { post_type: 'post-type', posts_per_page: -1, order: 'DESC' } %}
    {% for field in user.field %}
        <h1>{{ field.title }}</h1>
            <p>Welcome {{ user.display_name }}</p>              
                <table>
                    <thead>
                        <tr>
                            <td>Name</td>
                            <td>School</td>
                            <td>Visit Date</td>
                            <td>Action</td>
                        </tr>
                    </thead>
{% set enquiry = mb.get_posts( enq ) %}
    {% for post in enquiry %}
        {% if post.field_name.title == 'Post Title' %} //** Define post title.. not practical when adding more posts, ideal for solo page **\\
                    <tbody>
                        <tr>
                            <td>{{ post.parent_first_name }} {{ post.parent_last_name }}</td>
                            <td>{{ post.field_name.title }}</td>
                            <td>{{ post.visit_date | date( 'd-m-Y' ) }}</td>
                            <td><a href="{{ site.url }}/form-page/?rwmb_frontend_field_post_id={{ post.ID }}" class="table-link">Edit</a> | <a href="{{ post.url }}" class="table-link">View</a></td>
                        </tr>
                    </tbody>
                    {% else %}  
                <h3>There is Nothing to see Here Go back <a href="{{ site.url }}" class="table-link">Home</a></h3>  
            {% endif %}
        {% endfor %}
    </table> 
{% endfor %}
